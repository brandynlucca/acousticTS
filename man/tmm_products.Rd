% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmm-bistatic.R
\name{tmm_products}
\alias{tmm_products}
\title{Collect multiple post-processed products from one stored TMM solve}
\usage{
tmm_products(
  object,
  frequency = NULL,
  theta_body = NULL,
  phi_body = NULL,
  orientation = NULL,
  bistatic_summary = FALSE,
  include_grid = FALSE,
  n_theta = 91,
  n_phi = 181,
  n_psi = 181,
  sectors = NULL,
  drop_dB = 3
)
}
\arguments{
\item{object}{Scatterer-object previously evaluated with
\code{target_strength(..., model = "TMM", store_t_matrix = TRUE)}.}

\item{frequency}{Stored frequency (Hz) used when \code{bistatic_summary = TRUE}.}

\item{theta_body}{Incident polar angle (radians) for the monostatic and
bistatic products. Defaults to the stored TMM incident angle.}

\item{phi_body}{Incident azimuth angle (radians) for the monostatic and
bistatic products. Defaults to the stored TMM incident angle.}

\item{orientation}{Optional orientation distribution created by
\code{\link{tmm_orientation_distribution}}.}

\item{bistatic_summary}{Logical; include the output of
\code{\link{tmm_bistatic_summary}}.}

\item{include_grid}{Logical; keep the 2D scattering grid inside the bistatic
summary output.}

\item{n_theta}{Number of receive polar-angle samples for the bistatic
summary.}

\item{n_phi}{Number of receive azimuth samples for the bistatic summary.}

\item{n_psi}{Number of forward-centered angular samples used by the local
summary slices.}

\item{sectors}{Optional angular-sector table passed to
\code{\link{tmm_bistatic_summary}}.}

\item{drop_dB}{Positive dB drop used to define the backscatter-lobe width.}
}
\value{
A named list containing the requested post-processed TMM products.
}
\description{
Provides a higher-level convenience interface for the stored-block TMM
workflow. One call can return the monostatic scattering spectrum, an
orientation average, and a higher-level bistatic summary without rebuilding
the underlying T-matrix solve. For stored cylinders, the currently supported
products are the exact monostatic scattering spectrum and the corresponding
orientation-averaged monostatic outputs; cylinder bistatic summaries are
intentionally outside the public scope.
}
\seealso{
\code{\link{tmm_scattering}},
\code{\link{tmm_average_orientation}},
\code{\link{tmm_bistatic_summary}}
}
