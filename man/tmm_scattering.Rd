% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmm-scattering.R
\name{tmm_scattering}
\alias{tmm_scattering}
\title{Evaluate scattering from a stored TMM object}
\usage{
tmm_scattering(
  object,
  theta_body = NULL,
  phi_body = NULL,
  theta_scatter = NULL,
  phi_scatter = NULL
)
}
\arguments{
\item{object}{Scatterer-object previously evaluated with
\code{target_strength(..., model = "TMM", store_t_matrix = TRUE)}.}

\item{theta_body}{Incident polar angle (radians). Defaults to the stored TMM
incident angle.}

\item{phi_body}{Incident azimuth angle (radians). Defaults to the stored TMM
incident angle.}

\item{theta_scatter}{Receive polar angle (radians). Defaults to the exact
monostatic direction, \code{pi - theta_body}.}

\item{phi_scatter}{Receive azimuth angle (radians). Defaults to the exact
monostatic direction, \code{phi_body + pi}.}
}
\value{
A data frame with the frequency, complex scattering amplitude, the
corresponding differential cross section, and its level in dB.
}
\description{
Evaluates the far-field scattering amplitude from a previously computed
\code{TMM} object using the stored transition-matrix blocks. This allows the same
retained
modal operator to be reused for arbitrary single-target incident and
receive-angle combinations without rebuilding the boundary solve. In the
current package build, stored cylinders are limited to exact monostatic
reuse through this helper; public general-angle cylinder bistatic evaluation
remains outside scope.
}
\seealso{
\code{\link[=target_strength]{target_strength()}}, \code{\link{tmm_average_orientation}}
}
