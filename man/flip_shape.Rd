% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_manipulation.R
\name{flip_shape}
\alias{flip_shape}
\title{Flip a stored shape or scatterer profile across the x or z axis}
\usage{
flip_shape(
  object,
  axis = c("x", "z"),
  component = NULL,
  containment = c("warn", "error", "ignore")
)
}
\arguments{
\item{object}{Shape or Scatterer object.}

\item{axis}{Flip axis. Use \code{"x"} to reverse nose/tail orientation while
keeping the existing x grid, or \code{"z"} to mirror the profile vertically.}

\item{component}{Optional component name for scatterers. Defaults to the
primary geometry (\code{"body"} for most scatterers and \code{"shell"} for \code{ESS}).}

\item{containment}{Containment policy used when a moved swimbladder or
backbone is checked against its body: \code{"warn"}, \code{"error"}, or \code{"ignore"}.}
}
\value{
The modified object, returned as the same broad object type.
}
\description{
Reverse axial orientation (\code{axis = "x"}) or mirror the geometry
dorsoventrally (\code{axis = "z"}) without manually editing the position matrix.
}
\examples{
shape_obj <- arbitrary(
  x_body = c(0, 0.01, 0.02, 0.03),
  radius_body = c(0, 0.004, 0.002, 0)
)
flipped_shape <- flip_shape(shape_obj, axis = "x")
head(extract(flipped_shape, c("position_matrix", "zU")))

}
\seealso{
\code{\link[=translate_shape]{translate_shape()}}, \code{\link[=reanchor_shape]{reanchor_shape()}}, \code{\link[=inflate_shape]{inflate_shape()}},
\code{\link[=smooth_shape]{smooth_shape()}}
}
\keyword{manipulator}
\keyword{shape}
