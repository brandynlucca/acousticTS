% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acoustics.R
\name{liquid_spheroidal_kernels}
\alias{liquid_spheroidal_kernels}
\title{Compute the kernel regular (\eqn{K_{nl}^{m(1)}}) and scattering
(\eqn{K_{nl}^{m(3)}}) matrices for a liquid spheroid scattering}
\usage{
liquid_spheroidal_kernels(
  m_max,
  n_max,
  chi_sw,
  chi_body,
  theta,
  xi,
  density_scatterer,
  density_sw,
  n_integration = 128
)
}
\arguments{
\item{m_max}{Maximum azimuthal order m (>= 0) used to iterate the
modal series solution.}

\item{n_max}{Maximum degree n (>= m_max).}

\item{chi_sw}{Reduced frequency for the surrounding medium.}

\item{chi_body}{Reduced frequency for the body / interior.}

\item{xi}{Prolate spheroidal coordinate (\eqn{|{\xi}| \geq 1}).}

\item{density_scatterer}{Density of the scatterer (\eqn{kg~m^{-3}}).}

\item{density_sw}{Density of the surrounding medium (\eqn{m~s^{-1}}).}

\item{n_integration}{Number of integration points used for Gauss-Legendre
quadrature.}

\item{theta_body}{Orientation of the prolate spheroid relative to the
incident sound wave.}
}
\value{
A named list with two elements:
\item{K1_kernel}{list of length \code{m_max + 1}; each element is a complex
matrix (rows = l, cols = n) containing \eqn{K^{m(1)}_{nl}} for that m.}
\item{K3_kernel}{list of length \code{m_max + 1}; each element is a complex
matrix (rows = l, cols = n) containing \eqn{K^{m(3)}_{nl}} for that m.}
}
\description{
Compute the kernel regular (\eqn{K_{nl}^{m(1)}}) and scattering
(\eqn{K_{nl}^{m(3)}}) matrices for a liquid spheroid scattering
}
\details{
This wrapper calls the underlying C++ implementation to produce the kernel
matrices used to form the linear systems for the scattering coefficients
\eqn{A_{mn}} specific liquid-filled prolate spheroids.
}
