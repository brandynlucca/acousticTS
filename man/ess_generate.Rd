% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_scatterer.R
\name{ess_generate}
\alias{ess_generate}
\title{Generate an ESS-class object}
\usage{
ess_generate(
  shape = NULL,
  x_body = NULL,
  y_body = NULL,
  z_body = NULL,
  radius_shell = NULL,
  shell_thickness = NULL,
  g_fluid = NULL,
  density_fluid = NULL,
  h_fluid = NULL,
  sound_speed_fluid = NULL,
  g_shell = NULL,
  density_shell = NULL,
  h_shell = NULL,
  sound_speed_shell = NULL,
  E = NULL,
  G = NULL,
  K = NULL,
  nu = NULL,
  theta_shell = pi/2,
  ID = NULL,
  theta_units = "radians",
  length_units = "m"
)
}
\arguments{
\item{shape}{Pre-built \code{Shape} object describing the outer shell geometry. If
omitted, explicit shell profile coordinates such as \code{x_body}, \code{y_body},
and \code{z_body} are treated as the manual geometry pathway. Legacy character
dispatch such as \code{"sphere"} is retained only for backward compatibility and
is now deprecated.}

\item{x_body}{Vector containing x-axis body (m) shape data.}

\item{y_body}{Vector containing y-axis body (m) shape data.}

\item{z_body}{Vector containing z-axis body (m) shape data.}

\item{radius_shell}{Radius of shell (m).}

\item{shell_thickness}{Optional shell thickness (m).}

\item{g_fluid}{Optional density contrast for fluid-like body.}

\item{density_fluid}{Optional density for fluid-like body (kg/m³).}

\item{h_fluid}{Optional sound speed contrast for fluid-like body.}

\item{sound_speed_fluid}{Optional sound speed for fluid-like body (m/s).}

\item{g_shell}{Density contrast for the shell.}

\item{density_shell}{Optional density for the shell (kg/m³).}

\item{h_shell}{Sound speed contrast for the shell.}

\item{sound_speed_shell}{Optional sound speed for the shell (m/s).}

\item{E}{Young's modulus (Pa) of the shell material.}

\item{G}{Shear modulus (Pa) of the shell material.}

\item{K}{Bulk modulus (Pa) of the shell material.}

\item{nu}{Poisson's ratio (Dimensionless) of the shell material.}

\item{theta_shell}{Object orientation relative to incident sound wave.}

\item{ID}{Optional metadata entry.}

\item{theta_units}{Compatibility argument. Scatterer constructors now assume
radians and ignore non-SI alternatives.}

\item{length_units}{Compatibility argument. Scatterer constructors now assume
meters and ignore non-SI alternatives.}
}
\value{
ESS-class object
}
\description{
Generate an ESS-class object
}
\details{
The preferred workflow is to build the shell geometry first as a \code{Shape}
object and pass it through \code{shape}, or to supply explicit shell profile
coordinates directly. Character-based shape dispatch remains available only
as a compatibility pathway and is now deprecated. Material properties for
both the shell and the inner fluid may be supplied either as contrasts or as
absolute values, but each property pair must use only one representation.

Scatterer constructors store geometry in meters and orientations in radians.
\code{length_units} and \code{theta_units} are retained as compatibility arguments, but
non-SI values are normalized to the package-standard representation.
}
\examples{
shell <- sphere(radius_body = 0.03, n_segments = 80)
ess_generate(
  shape = shell,
  shell_thickness = 0.001,
  density_shell = 1050,
  sound_speed_shell = 2350,
  density_fluid = 1030,
  sound_speed_fluid = 1500,
  E = 3.5e9,
  nu = 0.34
)

}
\seealso{
\code{\link{ESS}}
}
\keyword{scatterer_type_generation}
