% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmm-scattering.R, R/tmm-orientation.R
\name{tmm_average_orientation}
\alias{tmm_average_orientation}
\title{Orientation-average scattering from a stored TMM object}
\usage{
tmm_average_orientation(
  object,
  theta_body = NULL,
  weights = NULL,
  phi_body = pi,
  theta_scatter = NULL,
  phi_scatter = NULL,
  distribution = NULL
)

tmm_average_orientation(
  object,
  theta_body = NULL,
  weights = NULL,
  phi_body = pi,
  theta_scatter = NULL,
  phi_scatter = NULL,
  distribution = NULL
)
}
\arguments{
\item{object}{Scatterer-object previously evaluated with
\code{target_strength(..., model = "TMM", store_t_matrix = TRUE)}.}

\item{theta_body}{Numeric vector of incident polar angles (radians).}

\item{weights}{Optional numeric vector of averaging weights. When omitted,
equal weights are used.}

\item{phi_body}{Incident azimuth angle(s) (radians). Either scalar or the
same length as \code{theta_body}. Defaults to \code{pi}.}

\item{theta_scatter}{Receive polar angle(s) (radians). Either scalar or the
same length as \code{theta_body}. Defaults to the monostatic direction.}

\item{phi_scatter}{Receive azimuth angle(s) (radians). Either scalar or the
same length as \code{theta_body}. Defaults to the monostatic direction.}

\item{distribution}{Optional orientation distribution created by
\code{\link{tmm_orientation_distribution}}. When supplied, it overrides the direct
\code{theta_body}, \code{weights}, and \code{phi_body} inputs.}
}
\value{
A data frame containing the frequency, the orientation-averaged
differential backscattering cross section and the corresponding
orientation-averaged target strength.

A data frame containing the frequency, the orientation-averaged
differential backscattering cross section and the corresponding
orientation-averaged target strength.
}
\description{
Reuses the stored TMM blocks to average the differential scattering cross
section over a user-supplied set of incident orientations. By default the
receive direction is taken to be the exact monostatic direction for each
supplied orientation.

Reuses the stored TMM blocks to average the differential scattering cross
section over a user-supplied set of incident orientations. By default the
receive direction is taken to be the exact monostatic direction for each
supplied orientation.
}
\seealso{
\code{\link{tmm_scattering}}

\code{\link{tmm_scattering}}
}
