% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmm-scattering.R, R/tmm-orientation.R
\name{tmm_orientation_distribution}
\alias{tmm_orientation_distribution}
\title{Build an orientation distribution for stored TMM post-processing}
\usage{
tmm_orientation_distribution(
  distribution = c("uniform", "normal", "truncated_normal", "quadrature", "pdf"),
  theta_body = NULL,
  weights = NULL,
  pdf = NULL,
  phi_body = pi,
  mean_theta = pi/2,
  sd_theta = pi/12,
  lower = 0,
  upper = pi,
  n_theta = 91
)

tmm_orientation_distribution(
  distribution = c("uniform", "normal", "truncated_normal", "quadrature", "pdf"),
  theta_body = NULL,
  weights = NULL,
  pdf = NULL,
  phi_body = pi,
  mean_theta = pi/2,
  sd_theta = pi/12,
  lower = 0,
  upper = pi,
  n_theta = 91
)
}
\arguments{
\item{distribution}{Orientation-distribution type. One of \code{"uniform"},
\code{"normal"}, \code{"truncated_normal"}, \code{"quadrature"}, or \code{"pdf"}.}

\item{theta_body}{Optional numeric vector of incident polar angles (radians).
Required for the \code{"quadrature"} and \code{"pdf"} pathways.}

\item{weights}{Optional numeric quadrature weights paired with \code{theta_body}
for \code{distribution = "quadrature"}.}

\item{pdf}{Optional user-supplied density over \code{theta_body} for
\code{distribution = "pdf"}. This can be either a numeric vector the same
length as \code{theta_body} or a function evaluated at \code{theta_body}.}

\item{phi_body}{Incident azimuth angle(s) (radians). Either scalar or the
same length as the resolved \code{theta_body} grid.}

\item{mean_theta}{Mean angle (radians) for the normal-family distributions.}

\item{sd_theta}{Standard deviation (radians) for the normal-family
distributions.}

\item{lower}{Lower bound (radians) for the uniform and truncated-normal
distributions.}

\item{upper}{Upper bound (radians) for the uniform and truncated-normal
distributions.}

\item{n_theta}{Number of grid points for the analytic distributions.}
}
\value{
A data frame with normalized orientation weights and class
\code{"TMMOrientationDistribution"}.

A data frame with normalized orientation weights and class
\code{"TMMOrientationDistribution"}.
}
\description{
Creates a validated set of incident angles and normalized weights that can be
reused by \code{\link{tmm_average_orientation}} or
\code{\link{tmm_products}}. The distributions
defined here are distributions in \code{theta_body} itself rather than isotropic
solid-angle distributions.

Creates a validated set of incident angles and normalized weights that can be
reused by \code{\link{tmm_average_orientation}} or
\code{\link{tmm_products}}. The distributions
defined here are distributions in \code{theta_body} itself rather than isotropic
solid-angle distributions.
}
\seealso{
\code{\link{tmm_average_orientation}},
\code{\link{tmm_products}}

\code{\link{tmm_average_orientation}},
\code{\link{tmm_products}}
}
