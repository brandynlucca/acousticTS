% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_manipulation.R
\name{inflate_shape}
\alias{inflate_shape}
\title{Locally widen, pinch, or taper a stored shape profile}
\usage{
inflate_shape(
  object,
  x_range = NULL,
  scale = 1,
  component = NULL,
  axis = c("radius", "width", "height"),
  profile = c("cosine", "linear", "box"),
  containment = c("warn", "error", "ignore")
)
}
\arguments{
\item{object}{Shape or Scatterer object.}

\item{x_range}{Optional axial interval over which the local manipulation is
applied.}

\item{scale}{Positive local scale factor.}

\item{component}{Optional component name for scatterers. Defaults to the
primary geometry (\code{"body"} for most scatterers and \code{"shell"} for \code{ESS}).}

\item{axis}{Which profile dimension to scale.}

\item{profile}{Local window profile. \code{"cosine"} gives a smooth bump centered
inside \code{x_range}, \code{"linear"} gives a triangular bump, and \code{"box"} applies a
uniform factor inside the interval.}

\item{containment}{Containment policy used when a moved swimbladder or
backbone is checked against its body: \code{"warn"}, \code{"error"}, or \code{"ignore"}.}
}
\value{
The modified object. \code{Shape} inputs that are locally reshaped are
returned as \code{Arbitrary} shapes.
}
\description{
Apply a localized scaling window to a stored profile. Values of \code{scale > 1}
inflate the selected region, while \code{scale < 1} pinch or taper it. For
canonical \code{Shape} objects, the result is returned as an \code{Arbitrary} shape
because the edited profile is no longer guaranteed to preserve the canonical
class geometry.
}
\examples{
shape_obj <- cylinder(length_body = 0.05, radius_body = 0.003, n_segments = 80)
pinched_shape <- inflate_shape(
  shape_obj,
  x_range = c(0.015, 0.035),
  scale = 0.7
)
max(extract(pinched_shape, c("shape_parameters", "max_radius")))

}
\seealso{
\code{\link[=smooth_shape]{smooth_shape()}}, \code{\link[=resample_shape]{resample_shape()}}, \code{\link[=flip_shape]{flip_shape()}}, \code{\link[=brake]{brake()}},
\code{\link[=reforge]{reforge()}}
}
\keyword{manipulator}
\keyword{shape}
