% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_manipulation.R
\name{smooth_shape}
\alias{smooth_shape}
\title{Smooth a stored shape or scatterer profile}
\usage{
smooth_shape(
  object,
  span = 5,
  component = NULL,
  preserve_ends = TRUE,
  containment = c("warn", "error", "ignore")
)
}
\arguments{
\item{object}{Shape or Scatterer object.}

\item{span}{Centered moving-average span. Even values are rounded up to the
next odd integer.}

\item{component}{Optional component name for scatterers. Defaults to the
primary geometry (\code{"body"} for most scatterers and \code{"shell"} for \code{ESS}).}

\item{preserve_ends}{Logical; whether to keep the first and last profile
points fixed.}

\item{containment}{Containment policy used when a moved swimbladder or
backbone is checked against its body: \code{"warn"}, \code{"error"}, or \code{"ignore"}.}
}
\value{
The modified object. \code{Shape} inputs that are smoothed are returned as
\code{Arbitrary} shapes.
}
\description{
Smooth the stored geometry using a centered moving-average filter applied to
profile coordinates. This is useful for cleaning digitized outlines before
canonicalization or model runs. For canonical \code{Shape} objects, the result is
returned as an \code{Arbitrary} shape because the edited profile is no longer
guaranteed to preserve the canonical class geometry.
}
\examples{
shape_obj <- arbitrary(
  x_body = c(0, 0.01, 0.02, 0.03, 0.04),
  zU_body = c(0, 0.003, 0.006, 0.0035, 0),
  zL_body = c(0, -0.0025, -0.0055, -0.003, 0)
)
smoothed_shape <- smooth_shape(shape_obj, span = 3)
extract(smoothed_shape, c("shape_parameters", "n_segments"))

}
\seealso{
\code{\link[=inflate_shape]{inflate_shape()}}, \code{\link[=resample_shape]{resample_shape()}}, \code{\link[=brake]{brake()}}, \code{\link[=reforge]{reforge()}}
}
\keyword{manipulator}
\keyword{shape}
