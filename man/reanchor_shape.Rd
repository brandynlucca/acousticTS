% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_manipulation.R
\name{reanchor_shape}
\alias{reanchor_shape}
\title{Re-anchor a stored shape or scatterer component along the x axis}
\usage{
reanchor_shape(
  object,
  anchor = c("nose", "center", "tail", "max_x", "min_x"),
  at = 0,
  component = NULL,
  containment = c("warn", "error", "ignore")
)
}
\arguments{
\item{object}{Shape or Scatterer object.}

\item{anchor}{Anchor location used to define the translation target. \code{nose}
is treated as the maximum stored x position and \code{tail} as the minimum.}

\item{at}{Target x location (m).}

\item{component}{Optional component name for scatterers. Defaults to the
primary geometry (\code{"body"} for most scatterers and \code{"shell"} for \code{ESS}).}

\item{containment}{Containment policy used when a moved swimbladder or
backbone is checked against its body: \code{"warn"}, \code{"error"}, or \code{"ignore"}.}
}
\value{
The modified object, returned as the same broad object type.
}
\description{
Translate a shape or scatterer component so that its nose, center, or tail
lies at a specified x position.
}
\examples{
shape_obj <- prolate_spheroid(length_body = 0.04, radius_body = 0.004)
centered_shape <- reanchor_shape(shape_obj, anchor = "center", at = 0)
range(extract(centered_shape, c("position_matrix", "x")))

}
\seealso{
\code{\link[=translate_shape]{translate_shape()}}, \code{\link[=brake]{brake()}}, \code{\link[=reforge]{reforge()}}, \code{\link[=extract]{extract()}}
}
\keyword{manipulator}
\keyword{shape}
