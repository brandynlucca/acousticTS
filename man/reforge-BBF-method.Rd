% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scatterer_reforge.R
\name{reforge,BBF-method}
\alias{reforge,BBF-method}
\title{Reforge BBF-class object.}
\usage{
\S4method{reforge}{BBF}(
  object,
  body_scale = NULL,
  body_target = NULL,
  backbone_scale = NULL,
  backbone_target = NULL,
  isometric_body = TRUE,
  isometric_backbone = TRUE,
  maintain_ratio = TRUE,
  n_segments_body = NULL,
  n_segments_backbone = NULL,
  containment = c("warn", "error", "ignore")
)
}
\arguments{
\item{object}{BBF-class object.}

\item{body_scale}{Proportional scaling to the body length, width, and height
dimensions. When a single value is supplied, all dimensions are scaled
uniformly. Otherwise, this input must be a named numeric vector.}

\item{body_target}{Target dimensions (m) for the body length, width, and
height dimensions. This input must be a named numeric vector.}

\item{backbone_scale}{Proportional scaling to the backbone length, width, and
height dimensions. When a single value is supplied, all dimensions are
scaled uniformly. Otherwise, this input must be a named numeric vector.}

\item{backbone_target}{Target dimensions (m) for the backbone length, width,
and height dimensions. This input must be a named numeric vector.}

\item{isometric_body}{Logical; maintain isometric scaling for body.}

\item{isometric_backbone}{Logical; maintain isometric scaling for backbone.}

\item{maintain_ratio}{Maintain size ratio between body and backbone.}

\item{n_segments_body}{Number of points along the body profile.}

\item{n_segments_backbone}{Number of points along the backbone profile.}

\item{containment}{Containment policy for internal geometry checks. Use
\code{"warn"} to keep the current warning behavior, \code{"error"} to fail fast for
invalid internal geometries, or \code{"ignore"} to skip containment checks.}
}
\value{
Modified BBF-class object.
}
\description{
Resize a backboned-fish scatterer by rescaling the flesh body and elastic
backbone independently or together. The body follows the same profile-based
length/width/height scaling used by \code{SBF}, while the backbone retains the
same cylinder-style component bookkeeping and preserves its relative axial
start within the body when body length changes.
}
\keyword{internal}
