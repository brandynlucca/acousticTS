% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_manipulation.R
\name{translate_shape}
\alias{translate_shape}
\title{Translate a stored shape or scatterer component}
\usage{
translate_shape(
  object,
  x_offset = 0,
  y_offset = 0,
  z_offset = 0,
  component = NULL,
  containment = c("warn", "error", "ignore")
)
}
\arguments{
\item{object}{Shape or Scatterer object.}

\item{x_offset}{Along-axis translation (m).}

\item{y_offset}{Lateral translation (m). This is ignored for profile-style
geometries that do not store an explicit lateral centerline.}

\item{z_offset}{Vertical translation (m).}

\item{component}{Optional component name for scatterers. Defaults to the
primary geometry (\code{"body"} for most scatterers and \code{"shell"} for \code{ESS}).}

\item{containment}{Containment policy used when a moved swimbladder or
backbone is checked against its body: \code{"warn"}, \code{"error"}, or \code{"ignore"}.}
}
\value{
The modified object, returned as the same broad object type.
}
\description{
Shift a \code{Shape} object or one geometry-bearing component of a \code{Scatterer}
without rebuilding it from scratch. This is most useful for re-centering
profiles, aligning a stored body to a preferred axial origin, or nudging an
internal component before model comparisons.
}
\examples{
shape_obj <- cylinder(length_body = 0.05, radius_body = 0.003, n_segments = 40)
moved_shape <- translate_shape(shape_obj, x_offset = 0.01)
range(extract(moved_shape, c("position_matrix", "x")))

}
\seealso{
\code{\link[=reanchor_shape]{reanchor_shape()}}, \code{\link[=offset_component]{offset_component()}}, \code{\link[=brake]{brake()}}, \code{\link[=reforge]{reforge()}},
\code{\link[=extract]{extract()}}
}
\keyword{manipulator}
\keyword{shape}
