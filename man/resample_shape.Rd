% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape_manipulation.R
\name{resample_shape}
\alias{resample_shape}
\title{Resample a stored shape or scatterer profile to a new segment count}
\usage{
resample_shape(
  object,
  n_segments,
  component = NULL,
  containment = c("warn", "error", "ignore")
)
}
\arguments{
\item{object}{Shape or Scatterer object.}

\item{n_segments}{New number of intervals used to discretize the profile.}

\item{component}{Optional component name for scatterers. Defaults to the
primary geometry (\code{"body"} for most scatterers and \code{"shell"} for \code{ESS}).}

\item{containment}{Containment policy used when a moved swimbladder or
backbone is checked against its body: \code{"warn"}, \code{"error"}, or \code{"ignore"}.}
}
\value{
The modified object, returned as the same broad object type.
}
\description{
Re-discretize a shape or scatterer component along its x axis without
rebuilding it from scratch.
}
\examples{
shape_obj <- sphere(radius_body = 0.01, n_segments = 20)
shape_fine <- resample_shape(shape_obj, n_segments = 80)
extract(shape_fine, c("shape_parameters", "n_segments"))

}
\seealso{
\code{\link[=smooth_shape]{smooth_shape()}}, \code{\link[=inflate_shape]{inflate_shape()}}, \code{\link[=reforge]{reforge()}}, \code{\link[=extract]{extract()}}
}
\keyword{manipulator}
\keyword{shape}
