# show(...) for SBF-class objects.

show(...) for SBF-class objects.

## Usage

``` r
sbf_show(object)
```

## Arguments

- object:

  SBF_class object.

## Value

Called for its side effect of printing a formatted summary; invisibly
returns `NULL`.
