# show(...) for ESS-class objects.

show(...) for ESS-class objects.

## Usage

``` r
ess_show(object)
```

## Arguments

- object:

  ESS-class object.

## Value

Called for its side effect of printing a formatted summary; invisibly
returns `NULL`.
