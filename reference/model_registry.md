# Model registry: maps model names to their functions and related initializers

Model registry: maps model names to their functions and related
initializers

## Usage

``` r
model_registry
```

## Format

An object of class `list` of length 10.
