# Primary accessor function for dredging specific data from scatterer objects

Primary accessor function for dredging specific data from scatterer
objects

## Usage

``` r
extract(object, feature)
```

## Arguments

- object:

  Scatterer-class object.

- feature:

  Feature of interest (e.g. body).
