# show(...) for GAS_class objects

show(...) for GAS_class objects

## Usage

``` r
gas_show(object)
```

## Arguments

- object:

  GAS-class object

## Value

Called for its side effect of printing a formatted summary; invisibly
returns `NULL`.
