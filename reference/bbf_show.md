# show(...) for BBF-class objects.

show(...) for BBF-class objects.

## Usage

``` r
bbf_show(object)
```

## Arguments

- object:

  BBF-class object.

## Value

Called for its side effect of printing a formatted summary; invisibly
returns `NULL`.
