# Discretize vector into separate intervals of different length

Discretize vector into separate intervals of different length

## Usage

``` r
segmentize(x1, x0)
```

## Arguments

- x1:

  Desired vector/interval

- x0:

  Original or initial vector/interval
