# show(...) for CAL-class objects.

show(...) for CAL-class objects.

## Usage

``` r
cal_show(object)
```

## Arguments

- object:

  CAL-class object.

## Value

Called for its side effect of printing a formatted summary; invisibly
returns `NULL`.
