# show(...) for FLS-class objects.

show(...) for FLS-class objects.

## Usage

``` r
fls_show(object)
```

## Arguments

- object:

  FLS-class object.

## Value

Called for its side effect of printing a formatted summary; invisibly
returns `NULL`.
